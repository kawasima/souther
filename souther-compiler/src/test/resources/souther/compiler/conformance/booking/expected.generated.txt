// --generate
// generated by `souther examples --generate`: 2 rows to fill what nothing covers.
// Replace each `<?>` with what the system actually answers.
example redeem
    | "party=1 <= x <= 4" : (Voucher("x"), Party(1)) -> <?>
    | "party=4 < x <= 8"  : (Voucher("x"), Party(5)) -> <?>
// generated by `souther examples --generate`: 2 rows to fill what nothing covers.
// Replace each `<?>` with what the system actually answers.
example seat
    | "party=1 <= x <= 4" : (Ticket("x"), Party(1)) -> <?>
    | "party=4 < x <= 8"  : (Ticket("x"), Party(5)) -> <?>
// --generate --boundaries
// generated by `souther examples --generate`: 2 rows to fill what nothing covers.
// Replace each `<?>` with what the system actually answers.
example book
    | (Seat(300), Party(1)) -> <?>
    | (Seat(1), Party(8))   -> <?>
// generated by `souther examples --generate`: 4 rows to fill what nothing covers.
// Replace each `<?>` with what the system actually answers.
example redeem
    | "party=4 < x <= 8" : (Voucher("x"), Party(5)) -> <?>
    | (Voucher("x"), Party(2))                      -> <?>
    | (Voucher("xx"), Party(1))                     -> <?>
    | (Voucher("x"), Party(8))                      -> <?>
// no row for `party = 4` in `redeem`: no row composed for it was seen reaching it, which does not make it unreachable
// no row for `party = 5` in `redeem`: no row composed for it was seen reaching it, which does not make it unreachable
// no row for `1 <= party < 4` in `redeem`: no row composed for it was seen reaching it, which does not make it unreachable
// no row for `5 < party <= 8` in `redeem`: no row composed for it was seen reaching it, which does not make it unreachable
// generated by `souther examples --generate`: 4 rows to fill what nothing covers.
// Replace each `<?>` with what the system actually answers.
example seat
    | "party=4 < x <= 8" : (Ticket("x"), Party(5)) -> <?>
    | (Ticket("x"), Party(2))                      -> <?>
    | (Ticket("xx"), Party(1))                     -> <?>
    | (Ticket("x"), Party(8))                      -> <?>
// no row for `party = 4` in `seat`: no row composed for it was seen reaching it, which does not make it unreachable
// no row for `party = 5` in `seat`: no row composed for it was seen reaching it, which does not make it unreachable
// no row for `1 <= party < 4` in `seat`: no row composed for it was seen reaching it, which does not make it unreachable
// no row for `5 < party <= 8` in `seat`: no row composed for it was seen reaching it, which does not make it unreachable
