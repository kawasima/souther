// --generate
// generated by `souther examples --generate`: 5 rows to fill what nothing covers.
// Replace each `<?>` with what the system actually answers.
example grantLeave
    | "worked=5 <= x <= 7" : (WorkDays(5), Rate(0m))   -> <?>
    | "rate=0.8 <= x <= 1" : (WorkDays(1), Rate(0.8m)) -> <?>

example judgeHours
    | "hours=45 < x <= 100"  : (Standard, Hours(46))  -> <?>
    | "hours=100 < x <= 200" : (Standard, Hours(101)) -> <?>
    | "agreement=Extended"   : (Extended, Hours(0))   -> <?>
// nothing offers a row for `a decision rule of grantLeave` in `grantLeave`: something was seen standing in this rule while it was being settled, and nothing turns what a search stood there into a row an author can complete
// nothing offers a row for `a decision rule of judgeHours` in `judgeHours`: something was seen standing in this rule while it was being settled, and nothing turns what a search stood there into a row an author can complete
// --generate --boundaries
// generated by `souther examples --generate`: 15 rows to fill what nothing covers.
// Replace each `<?>` with what the system actually answers.
example grantLeave
    | (WorkDays(5), Rate(0.8m)) -> <?>
    | (WorkDays(4), Rate(0.8m)) -> <?>
    | (WorkDays(6), Rate(0.8m)) -> <?>
    | (WorkDays(1), Rate(0.4m)) -> <?>
    | (WorkDays(7), Rate(0m))   -> <?>
    | (WorkDays(1), Rate(1m))   -> <?>

example judgeHours
// fills hours=0 <= x <= 45
// fills agreement=Standard
    | (Standard, Hours(0))                          -> <?>
    | "hours=45 < x <= 100" : (Standard, Hours(46)) -> <?>
    | (Extended, Hours(101))                        -> <?>
    | (Extended, Hours(100))                        -> <?>
    | (Extended, Hours(102))                        -> <?>
    | (Extended, Hours(99))                         -> <?>
    | (Standard, Hours(45))                         -> <?>
    | (Standard, Hours(47))                         -> <?>
    | (Standard, Hours(200))                        -> <?>
// nothing offers a row for `a decision rule of grantLeave` in `grantLeave`: something was seen standing in this rule while it was being settled, and nothing turns what a search stood there into a row an author can complete
// nothing offers a row for `a decision rule of judgeHours` in `judgeHours`: something was seen standing in this rule while it was being settled, and nothing turns what a search stood there into a row an author can complete
